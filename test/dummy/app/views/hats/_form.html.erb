<%= form_with(model: hat) do |form| %>
  <% if hat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hat.errors.count, "error") %> prohibited this hat from being saved:</h2>

      <ul>
        <% hat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :color, style: "display: block" %>
    <%= form.text_field :color %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
